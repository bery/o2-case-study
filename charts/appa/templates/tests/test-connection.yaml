apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appa.fullname" . }}-test-connection"
  labels:
{{ include "appa.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "appa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
